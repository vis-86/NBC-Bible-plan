# AI Factory Configuration
# Auto-generated by /aif. Edit as needed.

language:
  # Language for AI responses in chat
  ui: ru
  # Language for generated artifacts (docs, plans, etc.)
  artifacts: ru
  # Technical terms: keep in English even in Russian mode
  technical_terms: keep

paths:
  description: .ai-factory/DESCRIPTION.md
  architecture: .ai-factory/ARCHITECTURE.md
  docs: docs/
  roadmap: .ai-factory/ROADMAP.md
  research: .ai-factory/RESEARCH.md
  rules_file: .ai-factory/RULES.md
  plan: .ai-factory/PLAN.md
  plans: .ai-factory/plans/
  fix_plan: .ai-factory/FIX_PLAN.md
  security: .ai-factory/SECURITY.md
  references: .ai-factory/references/
  patches: .ai-factory/patches/
  evolutions: .ai-factory/evolutions/
  evolution: .ai-factory/evolution/
  specs: .ai-factory/specs/
  rules: .ai-factory/rules/

workflow:
  auto_create_dirs: true
  # Slug format for plan IDs (e.g. "add-calendar-feature")
  plan_id_format: slug
  # When analyzing codebase, update ARCHITECTURE.md
  analyze_updates_architecture: true
  # After architecture changes, suggest ROADMAP.md updates
  architecture_updates_roadmap: true
  # Verification strictness: normal | strict
  verify_mode: normal

git:
  enabled: true
  # Base branch for new feature branches
  base_branch: main
  # Whether /aif-plan full creates a new branch automatically
  create_branches: true
  branch_prefix: feature/
  skip_push_after_commit: false

rules:
  base: .ai-factory/rules/base.md
