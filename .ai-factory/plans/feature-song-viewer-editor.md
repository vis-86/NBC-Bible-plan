# Song viewer/editor — остаток работ

Источник требований: `docs/song-viewer-spec.md`. Milestones и порядок: `.ai-factory/ROADMAP.md`.
Ветка: `chordpro-viewer`. История этапов 1 и 3: `.ai-factory/plans/archive/song-viewer-stage-1-3.md`.

Этот файл — **контекст и инварианты для оставшихся этапов**, а не пошаговый роадмап.
Детальный план текущего этапа живёт отдельным файлом в `.ai-factory/plans/` и ссылается сюда.

> Номер этапа ≠ номер раздела спеки. Спека — документ с разделами §0–§14. Не открывай «§N» по номеру этапа.

---

## Что уже сделано (не переделывать)

**Этап 1 — уборка мёртвого кода** (`7b66d79`, `0f21ce9`). Табличный рендер-путь,
`structurizeSong`, `decomposeLine`, мёртвые пропы `SongBlock` удалены (−642 строки).
Рендер-путь один: `SongView` → `ChordProHtmlColumn` → `LineRenderer` → `ChordRenderer`.
`getSongsList` запрашивает `tempo`/`time`.

**Этап 3 — раскладка и настройки отображения** (`c59c19a`, `8a8d135`, `f91c178`, `593da5b`,
`ed5c824`, `4515332`, `8fce521`, `6591d12`, `854332a`, `d11b9e3`). Итог:

- Типографика идёт из CSS-переменных на `.cproSongBody` (`--lyric-size`, `--line-height`,
  `--section-gap`, `--col-count`); inline-`font-size` и проп `fontSize` в цепочке нет.
  «Только текст» — атрибут `data-chords="off"` + `.chord{display:none}`.
- `useSongViewSettings` — один JSON-ключ `songs:view-settings`
  (`mode`/`columns`/`fontSize`/`density`/`showChords`/`showHeader`), клампы, миграция со
  старого `songs:font-size`, устойчивость к битому JSON. Панель — `SongViewSettings`.
  Раскладочные контролы рендерятся только при `SONG_WIDE_LAYOUT_QUERY` (≥640px), и на узком
  экране режим принудительно приводится к `scroll`.
- Один multicol-поток (`SongBlock.tsx` удалён), режимы `sheets` (листы вниз) и `paged`
  (горизонтальные страницы, клавиши/педали/тап по третям, индикатор «N / M»).
  Чистые функции — `lib/sheets.ts` (`pitch`, `sheetCount`, `sheetPageHeight`, `nextPageDelta`),
  измерения — `hooks/useSheets.ts` / `hooks/usePagedFlow.ts`.
- Оболочка дашборда расширена до 48rem (`--app-shell-max-w`, класс `.app-shell-width`).
- E2E-регрессия раскладки — `e2e/layout/song-layout.spec.ts` (9 тестов, один логин на спек:
  прод rate-limit 10 попыток / 15 мин на IP).

---

## Решения, закрытые окончательно

1. **Идентификатор песни — integer `songs.id`.** Миграцию на `slug` не делать ни на одном
   этапе (обоснование — спека §7). Устаревшие комментарии про «id из имени файла» можно
   поправить текстом, поведение — нет.
2. **Настройки раскладки — свойство устройства**, один localStorage-ключ `songs:view-settings`.
   Тональность и пометки — свойство музыканта (спека §7), см. ниже.
3. **Типографику аккордов не трогать** без явной задачи: `tokenizeChord` в `ChordRenderer.tsx`
   и `chordSign*` в `songs.css` прошли ручную проверку на реальном корпусе (спека §3.4).
4. **Дефолт `mode` — `scroll`** на любом устройстве; авто-выбора по ширине нет.
   В `sheets`/`paged` авто-скрытие шапки отключено (смена высоты вьюпорта пересобирает листы).

## Персист пользовательских данных (сквозной вопрос M4/M5/M10)

Спека §7 требует, чтобы тональность, скорость автоскролла и штрихи ехали за музыкантом
между устройствами (Directus `song_user_state`). Но запись пользовательских данных обязана
идти через write-ahead outbox (офлайн-инварианты `CLAUDE.md`), а обобщение outbox — это M6,
которого ещё нет, и BFF по песням сейчас только GET.

**Рабочее допущение:** до M6 такие данные персистятся устройство-локально (localStorage,
скоуп «песня + настройка»), слой персиста прячется за одним модулем с узким API, чтобы
подмена хранилища на outbox+Directus в M6 была локальной правкой. Синхронизация между
устройствами до M6 не заявляется пользователю.

---

## Оставшиеся этапы

### С требованиями в спеке

- **M4 — транспозиция и тональности.** Спека §10 целиком, §3.1 (классификация
  `Chord`/`Bar`/`Note` — без неё транспозиция ломает `[|]` и `[(пауза)]`), §2 (виды токенов
  в корпусе), §12 (критерии приёмки). Поле `songs.default_key` в проде **уже есть**
  (string, nullable, пустое у всех песен) и объявлено в `src/lib/directus-schema.ts`;
  в `songsServer.ts` пока не запрашивается. Пустой `default_key` ⇒ действует `song_key`.
  → детальный план: `.ai-factory/plans/feature-song-transpose.md`
- **M5 — автоскролл.** Спека §8 целиком, §4.5 (живёт только в режиме `scroll`, в
  `sheets`/`paged` кнопка скрыта), §11 (`prefers-reduced-motion`). Скорость — на песню.
- **M8 — печать и PDF.** Спека §9 целиком, §4.6 (разрывы). `@media print` в проекте нет вовсе.
- **M10 — рукописные пометки.** Спека §6 (единственное место с зумом; штрихи привязаны к
  якорю `sectionIndex`/`lineIndex`, не к экранным координатам), §7. `perfect-freehand` не
  установлен. Зависит от M6.

### Без требований в спеке — постановку собрать до реализации

Не изобретать требования. Дойдя до такого этапа — сформулировать вопросы и остановиться.

- **M2 — роли доступа.** В коде нет ничего (`app_roles`/`requireRole` не существуют).
  В проде две Directus-роли: «Чтец» и «Administrator». Решение Игоря от 2026-07-25: создавать
  и править сетлисты может только роль `musician_editor`.
  Открыто: как роль попадает в сессию (отдельная Directus-роль ↔ policy или поле профиля),
  кто назначает, как гейтить BFF-роуты, нужны ли роли помимо `musician_editor`.
  **Учесть:** права Directus — второй рубеж, а не контур контроля доступа: приложение ходит
  в Directus админ-клиентом через BFF, реальный гейт обязан быть в BFF-роутах.
- **M6 — обобщение outbox.** `OutboxRecord` (`src/shared/offline/db.ts`) жёстко под прогресс
  чтения (`op: 'single'|'batch'`, `dayIds`, `count`). Форма обобщения — архитектурное решение.
  Тронет офлайн-контур ⇒ полный production-контур верификации обязателен.
- **M7 — сетлисты.** Прод-Directus на 2026-07-25: коллекции `setlists`/`setlist_items`/
  `song_user_state` есть (uuid PK, FK на `songs` = integer, поле `user_id`). В policy `custom`:
  `setlists`/`setlist_items` — только `read`; `song_user_state` — CRUD своих записей
  (`user_id = $CURRENT_USER`), настроено полностью. Запись в сетлисты — только `musician_editor`
  (роли нет) ⇒ блокируется M2. Права на запись выдаёт Игорь вручную.
- **M9 — редактирование песен.** BFF только GET. Нужно: кто редактирует (M2), что именно
  редактируется, конфликты. Запись — через outbox (M6), не прямым POST.

---

## Правила работы (любой этап)

### Границы
- Только ветка `chordpro-viewer`. Не трогать `main`, `deploy/`, прод-конфиги, `.env*`.
- Никогда не выполнять `npm run deploy`. Прод-Directus (коллекции, поля, права, схема) —
  ручная работа Игоря.
- Держаться рамок этапа: попутные улучшения — текстом, а не коммитом.
- Проект на **Yarn 1**. `npm install` молча переписывает `yarn.lock` — не использовать.

### Секреты
- `.env.test` gitignored; в свежем клоне его нет. Нужен для тестов — создать с **фиктивными**
  значениями по `ENV_SETUP.md`. Не коммитить.
- Реальные секреты не печатать и не логировать; в выводе — маскировать (`sk-***`).

### Верификация (без этого этап не готов)
1. `yarn test` — всегда зелёный (baseline на 2026-07-25: **421 тест**).
2. `yarn lint` — на ветке унаследованный долг: **59 errors / 30 warnings**. Ноль недостижим и
   не требуется. Критерий: снять числа ДО работы, после правок они не выросли, в тронутых
   файлах ошибок нет.
3. `npx tsc --noEmit` — чисто.
4. Тронул offline/SW/кэширование/роутинг — дополнительно полный прод-контур:
   `yarn build` → просмотреть реальный `out/sw.js` → `yarn e2e:offline`. Dev-режим эти баги не ловит.
5. Playwright в чистом окружении: браузеры не ставятся через install зависимостей —
   `npx playwright install --with-deps chromium`. Не удалось прогнать — правка **не проверена**:
   зафиксировать `E2E НЕ ПРОГНАН: <причина>` и этап не закрывать.
6. При падении проверки чинить причину, а не тест.

### Когда останавливаться
- Архитектурная развилка с trade-off (а не техническая деталь) — не угадывать: варианты в
  `## NEEDS DECISION` детального плана этапа, перейти к следующей независимой задаче.
- Этап без раздела требований в спеке — требования не изобретать.
