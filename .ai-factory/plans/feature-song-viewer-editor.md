# Song viewer/editor — autonomous implementation plan

Источник требований: `docs/song-viewer-spec.md`. Ветка: `chordpro-viewer`.

Каждый запуск (в т.ч. автономный, из cloud routine) начинает с чистого чекаута — **этот файл единственный источник прогресса**, обновляй Progress Log перед завершением сессии.

> **Внимание: номер этапа в этом плане НЕ равен номеру раздела спеки.** Спека — это документ с разделами §0–§14, а не пошаговый роадмап. У каждого этапа ниже явно указано, какие разделы читать. Не открывай «§N» по номеру этапа.

## Правила для автономного агента

### Границы
- Работай ТОЛЬКО в ветке `chordpro-viewer`. Никогда не трогай `main`, `deploy/`, прод-конфиги, `.env*`.
- Никогда не выполняй `npm run deploy`. Никогда не меняй прод-Directus (коллекции, поля, права, схему) — это ручная работа Игоря.
- Не переписывай типографику аккордов (`tokenizeChord` в `ChordRenderer.tsx`, `chordSign*` в `songs.css`) без явной задачи — она прошла ручную проверку на реальном корпусе.
- Держись рамок задачи: попутные улучшения — записью в Progress Log, а не коммитом.
- Не добавляй абстракции, флаги конфигурации и обработку невозможных случаев без явного запроса.

### Секреты
- `.env.test` gitignored и в свежем клоне его нет. Если тесты его требуют — создай с **фиктивными** значениями по списку из `ENV_SETUP.md` (`NEXT_PUBLIC_DIRECTUS_URL`, `DIRECTUS_ADMIN_TOKEN`, `TELEGRAM_BOT_TOKEN`, `NEXT_PUBLIC_BASE_PATH`, `SESSION_SECRET`, `INVITE_ADMIN_SECRET`, `NEXT_PUBLIC_APP_URL`). Не коммить.
- Никогда не коммить и не печатать реальные секреты. Встретил в выводе — маскируй (`sk-***`) перед тем как класть куда-либо, включая Progress Log.

### Верификация (без этого этап не готов)
1. `npm run test` — всегда зелёный (на 2026-07-25: 67 файлов, 383 теста).
2. `npm run lint` — **линтер на ветке УЖЕ красный по унаследованному долгу: 59 errors, 30 warnings в 41 файле** (в основном `no-explicit-any`, `ban-ts-comment`, setState-in-effect). Добиться нуля НЕЛЬЗЯ и НЕ НУЖНО — это не твоя задача, чинить чужой долг = выход за рамки.
   Критерий: прогони `npm run lint` **до** начала работы, запомни числа; после правок числа не должны вырасти, а в файлах, которые ты трогал, ошибок быть не должно. Если базовое число изменилось не из-за тебя — зафиксируй в Progress Log.
3. Тронул offline/SW/кэширование/роутинг — дополнительно: `npm run build` → просмотреть сгенерированный `out/sw.js` → `npm run e2e:offline` (см. `CLAUDE.md`, dev-режим эти баги не ловит).
4. **Playwright в чистом окружении:** браузеры не ставятся через `npm ci`. Перед `npm run e2e:offline` выполни `npx playwright install --with-deps chromium`. Если установка не проходит (нет root, нет сети) — **правка НЕ считается проверенной**: не пиши «готово», зафиксируй в Progress Log строкой `E2E НЕ ПРОГНАН: <причина> — нужен локальный прогон Игоря` и не закрывай этап галочкой.
5. При падении проверки чини причину, а не тест.

### Когда останавливаться и спрашивать
- Архитектурная развилка с trade-off (а не техническая деталь) — НЕ угадывать. Записать варианты в Progress Log под заголовком `## NEEDS DECISION`, остановиться на этой задаче и перейти к следующей независимой.
- Этап, для которого в спеке нет раздела с требованиями (см. таблицу ниже) — НЕ придумывать требования. Собрать вопросы, вынести в `## NEEDS DECISION`, перейти дальше.
- Если тесты не проходят и причина не ясна за разумное время — зафиксируй что именно сломано; не коммить сломанное как готовое (допустим коммит с явной пометкой WIP).

### Завершение сессии
- Тесты зелёные → `git add`, conventional-commit, `git push origin chordpro-viewer`.
- Всегда дописывай запись в **начало** Progress Log: `### YYYY-MM-DD HH:MM UTC — <итог>` + что дальше + блокеры. Пушить лог даже если код заблокирован.
- Один заход = один осмысленный кусок (примерно один коммит). Не пытаться закрыть всё за раз, оставлять чистую точку остановки.

---

## Решение по идентификатору песни (закрыто, не пересматривать)

**Внешний идентификатор — integer `songs.id`.** Ранний черновик спеки требовал `slug`; решение от 2026-07-25 это отменило, спека `docs/song-viewer-spec.md` §7 обновлена и содержит обоснование. Поле `songs.slug` в схеме остаётся, но идентификатором не служит.

Миграцию `id` → `slug` **не делать ни на одном этапе**. Если встретишь в коде или комментариях следы обратного — это устаревшие комментарии (например в `types.ts`, `db.ts`, где написано, что id — «строка из имени файла»); их можно поправить текстом, но поведение не менять.

---

## Этап 1 — уборка мёртвого кода

Разделы спеки: §0 (что есть сейчас), §3 (модель рендера — контекст, зачем убираем второй путь).

Проверенная фактура (на 2026-07-25): `SongView.tsx:51` → `SongBlock` не передаёт `renderMode`; `ChordProHtmlColumn.tsx:141` дефолт `'inline'`; `shouldUseTableMode` вызывается только в ветке `'auto'`. Значит табличный путь недостижим.

- [x] **Коммит A** — `refactor(songs): drop dead render paths` (7b66d79)
  - Удалить файл `src/features/songs/components/render/TableLineRenderer.tsx`
  - В `ChordProHtmlColumn.tsx`: удалить импорт `TableLineRenderer`, функцию `shouldUseTableMode`, проп `renderMode` и всё ветвление по нему, проп `showDebugHeights` вместе с `useEffect`, измеряющим `offsetHeight`
  - В `songParser.ts`: удалить `structurizeSong`, `StructuredSection`, `StructuredSong`
  - В `chordProUtils.ts`: после двух пунктов выше `decomposeLine` (и тип `DecomposedLine`) остаются без единого потребителя — удалить. `escapeHtml` проверить отдельно грепом, он может использоваться ещё где-то
  - В `SongBlock.tsx`: удалить пропы `isActive`, `onClick` (в `SongView.tsx:51` не передаются) и завязанные на них классы `active`. **Проп `isContinuation` в `SongBlock` тоже мёртв — но не путать с `section.meta?.isContinuation` в `ChordProHtmlColumn.tsx:64`, это другое поле, его НЕ трогать.** Удалить и устаревший комментарий про «убрана autoscroll-логика», раз пропов больше нет
  - **Тесты:** `songParser.test.ts` импортирует `structurizeSong` (строка 5) и тестирует его (`describe` на строке 54) — удалить этот `describe`-блок и импорт вместе с функцией. Остальные тесты файла должны остаться зелёными без правок. Новых тестов не требуется (удаление недостижимого кода)

- [x] **Коммит B** — `fix(songs): request tempo and time in songs list query` (0f21ce9) — добавлен юнит-тест `songsServer.test.ts`

**Этап 1 завершён.** Мёртвый код удалён (−642 строки), рендер-путь один (`LineRenderer`). Типографику аккордов не трогали. 388 тестов зелёные.

---

## Этапы 2–10

Перед реализацией любого этапа:
1. Прочитать **указанные для него** разделы спеки (не по номеру этапа).
2. Прочитать текущее состояние затронутого кода — не полагаться на описания из старых сессий, код мог измениться.
3. Расписать этап на коммиты/файлы/тесты прямо в этом файле (заменить строку чек-листа подробным под-списком, как сделано для этапа 1), закоммитить обновление плана **отдельным коммитом** от кода.
4. Только потом писать код.

### Этапы с требованиями в спеке

- [ ] **Этап 3 — раскладка листами, колонки, настройки отображения**
  Спека: **§4 целиком** (колонки, режим `sheets`, три подводных камня, §4.6 разрывы), **§5** (размер шрифта, плотность, тумблер шапки), **§7** (таблица настроек и скоупов), **§3.3** (режим «только текст»), **§11** (доступность). Прототип раскладки — `docs/song-viewer-prototype.html`.

#### Фактура на 2026-07-25 (проверено чтением кода)

- Рендер-цепочка: `app/dashboard/song/page.tsx` → `SongView` (шапка + `.song-blocks-layout.single`) → `SongBlock` (обёртка `.song-block > .song-block-content{overflow-x:auto} > .cproColumn`, **по одной на блок**) → `ChordProHtmlColumn` (`.cproSongSection` + `LineRenderer`) → `ChordRenderer`.
- `fontSize` прокидывается пропом через 4 уровня и садится inline-стилем в двух местах: `ChordProHtmlColumn.tsx:43` (секция) и `LineRenderer.tsx:195` (строка). `hideChords` — пропом до `ChordRenderer`.
- `useSongFontSize`: ключ `songs:font-size`, диапазон 14–28, дефолт 17. Настроек кроме шрифта нет.
- **Прототип использует ту же legacy-разметку** (`chordWrapper`/`float`/`wordWrapper`), что и React-код. Значит токен-модель §3.1/§3.2 на этом этапе НЕ переписываем — этап чисто про раскладку вокруг существующего рендера.
- Блокирующее для колонок: multicol требует **один** поток со всеми секциями, а сейчас flex-колонка из N `.cproColumn` с `overflow-x:auto` на каждом блоке. DOM надо перестроить в один `.cproColumn` (как в прототипе); `.song-block-content{overflow-x:auto}` уходит — §11/§12 требуют отсутствия горизонтального скролла в reflow-модели.
- DOM-якорей песни снаружи фичи нет (`grep`: только `data-song-page*` в page.tsx) — перестройка разметки ничего не ломает.

#### Принятые решения (не развилки, зафиксировано)

1. Все настройки раскладки — **один** localStorage-ключ `songs:view-settings` (JSON), одноразовая миграция из `songs:font-size`. Отдельные ключи на каждую настройку = 6 ключей и 6 путей рассинхрона.
2. Дефолт `mode` — `scroll` на любом устройстве (переключатель явный, §4.5). Авто-выбора по ширине нет.
3. В `sheets`/`paged` авто-скрытие шапки (`useAutoHideOnScroll`) отключается: смена высоты вьюпорта = перестройка листов на каждый скролл. Экран освобождает тумблер «скрыть шапку» (§5).
4. `SongBlock` удаляется — после перехода на один поток он ничего не добавляет к `.cproSongSection`.

#### Коммиты

- [ ] **Коммит A** — `refactor(songs): drive song rendering from CSS variables`
  - `songs.css`: на корне `.cproSongBody` — `--lyric-size` (задаётся из JS), производные `--chord-size: calc(var(--lyric-size) * 0.85)`, `--line-height`, `--section-gap` (§5). `.cproSongLine` / `.chord` / `.cproSongSection` переводятся на них.
  - Убрать проп `fontSize` из `SongView`/`SongBlock`/`ChordProHtmlColumn`/`LineRenderer` и оба inline-`style`. Значение ставится один раз: `style={{ '--lyric-size': fontSize+'px' }}` на `.cproSongBody`.
  - `hideChords` → атрибут `data-chords="off"` на корне + `.chord { display: none }` (§3.3); проп убрать из всей цепочки до `ChordRenderer`. `aria-hidden` на аккорде в этом режиме (§11).
  - `break-inside: avoid` на `.cproSongLine` в дополнение к `.cproSongSection` (§4.6).
  - **Тесты:** новый `SongView.test.tsx` (jsdom): CSS-переменная стоит на корне; при `hideChords` есть `data-chords="off"` и `aria-hidden` на аккорде; в DOM нет inline `font-size`.

- [ ] **Коммит B** — `feat(songs): song view settings (density, chords, header, columns, mode)`
  - Новый `hooks/useSongViewSettings.ts`: `{ mode, columns, fontSize, density, showChords, showHeader }`, персист одним JSON-ключом, клампы (`fontSize` 12–32, §5), миграция со старого ключа, устойчивость к битому JSON. `useSongFontSize` удаляется вместе с тестом (его кейсы переезжают).
  - `SongFontSettings` → `SongViewSettings`: слайдер 12–32, сегменты плотности/режима/колонок, тумблеры аккордов и шапки. Тап-таргеты ≥44px (§11). «2 колонки» дизейблятся при `< 640px` с подсказкой «доступно на планшете» (§4.5).
  - `SongView`: `showHeader=false` убирает `<header>`; `density` → `data-density` на корне, CSS меняет `--line-height` 1.45/1.2 и `--section-gap`.
  - **Тесты:** `useSongViewSettings.test.ts` (дефолты, кламп границ 12/32, миграция `songs:font-size` → JSON, битый JSON → дефолты, недоступность localStorage); компонентный тест дизейбла «2 колонки» на узком экране.

- [ ] **Коммит C** — `feat(songs): single multicol flow and sheets layout`
  - `SongView` рендерит **один** `.cproColumn` со всеми секциями (`SongBlock.tsx` удалить, `parseSongBlocks` → массив секций для `ChordProHtmlColumn`). `columns: var(--col-count); column-gap: 2rem` — по прототипу.
  - Новый `lib/sheets.ts` — **чистые** функции: `pitch(clientWidth, columnGap)` = ширина + gap (подводный камень 1), `sheetCount(maxSectionRight, pitch)` = `max(1, ceil((maxRight-2)/pitch))` (подводный камень 3). Измерения передаются аргументами, DOM внутри нет.
  - Новый `hooks/useSheets.ts`: измеряет источник, строит N листов-«окон» (`.sheet` `content-box` + `.sheet-flow` с `transform: translateX(-i*pitch)`), перестраивает на смену колонок/шрифта (debounce 120мс)/плотности/режима «только текст»/resize/поворот (ResizeObserver + `orientationchange`). Клон живёт **внутри** контейнера с той же типографикой (подводный камень 2).
  - Высота листа: `--page-h` из высоты скролл-контейнера — `page.tsx` передаёт `viewportRef`; в `sheets`/`paged` `useAutoHideOnScroll` не подключается (решение 3).
  - CSS стыка §4.3: зазор 14px, волосяная линия, `padding-top: 18px`, номер «2 / 3» в углу. Токены только семантические (`--app-*`), никаких хардкод-шейдов.
  - **Тесты:** `sheets.test.ts` — шаг с gap и без, отсутствие хвостового пустого листа (`maxRight` ровно кратен шагу), одна страница при контенте уже экрана, защита от нулевой ширины.

- [ ] **Коммит D** — `feat(songs): paged mode with keyboard and tap paging`
  - Режим `paged`: тот же поток, `flow.scrollLeft += pitch` (§4.4), без клонирования.
  - Управление: `→`/пробел/`PageDown` вперёд, `←`/`PageUp` назад (Bluetooth-педали), тап по правой/левой трети, кнопки панели (≥44px), индикатор «2 / 3» — обязателен и в `sheets`, и в `paged`.
  - **Тесты:** unit на чистый резолвер клавиши → направление (`nextPageDelta(key)`), чтобы не тащить это в e2e.

- [ ] **Коммит E** — `test(songs): e2e regression for sheets and paged layout`
  - Новый `e2e/layout/song-layout.spec.ts` (тот же playwright-конфиг, отдельная папка от `e2e/offline`): ни одного пустого листа в комбинациях 1/2 колонки × шрифт 12/18/30; левый край содержимого одинаков на всех листах; объединение секций по листам = все секции песни без пересечений; низ последнего листа не обрезан; в `paged` шаг ровно `clientWidth + column-gap`; нет горизонтального скролла на 390px и 1024px (§12).
  - Требует прод-контур + `.env.test` с кредами тест-аккаунта. Если Playwright/креды недоступны — по правилу верификации №4 фиксируем `E2E НЕ ПРОГНАН` в Progress Log, этап галочкой не закрываем.

  **Верификация этапа:** `npm run test` + `npm run lint` (baseline снять ДО работы) на каждом коммите; роутинг/SW/офлайн не трогаются (данные песни не меняются, новых маршрутов нет) ⇒ полный прод-контур нужен только под коммит E.

- [ ] **Этап 4 — транспозиция и тональности**
  Спека: **§10 целиком** (три уровня тональности, хранение строкой, правила транспозиции токенов), **§3.1** (токен-модель — нужна классификация `Chord`/`Bar`/`Note`), **§2** (виды токенов в корпусе), **§12** (критерии приёмки по транспозиции).
  Ключевое: без классификации маркеров транспозиция начнёт ломать `[|]` и `[(пауза)]`. `@tonaljs` в зависимостях нет — перед установкой оценить по правилу «средства языка → существующий код → зрелая библиотека»; §10.2 требует энгармонию по целевой тональности, самописно это заметный объём, библиотека скорее оправдана.
  **Поле `songs.default_key` в проде УЖЕ ЕСТЬ** (проверено 2026-07-25: string, nullable, пустое у всех песен) и объявлено в `src/lib/directus-schema.ts`. Заводить его не нужно. Пустое значение ⇒ действует `song_key` (исходная) — фолбэк по §10.1 обязателен. В `songsServer.ts` поле пока не запрашивается — добавить в `fields` детали.

- [ ] **Этап 5 — автоскролл**
  Спека: **§8 целиком**, **§4.5** (автоскролл живёт только в режиме `scroll`, в `sheets`/`paged` кнопка скрыта), **§11** (`prefers-reduced-motion`).
  Персист скорости — на песню.

- [ ] **Этап 8 — печать и PDF**
  Спека: **§9 целиком**, **§4.6** (разрывы).
  Сейчас `@media print` в проекте нет вовсе.

- [ ] **Этап 10 — рукописные пометки**
  Спека: **§6 целиком** (единственное место с зумом; хранение штрихов через якорь `sectionIndex`/`lineIndex`, не экранные координаты), **§7** (скоуп `strokes` — на песню, на пользователе).
  `perfect-freehand` не установлен. Хранение в Directus ⇒ зависит от этапа 6 (outbox) и требует полей/прав — см. NEEDS DECISION ниже.

### Этапы БЕЗ требований в спеке — требования собрать до реализации

Разделов спеки под эти этапы нет. Их постановка родилась в обсуждении, которого у автономной сессии нет. **Не изобретать требования.** Дойдя до такого этапа: сформулировать конкретные вопросы, записать в `## NEEDS DECISION`, перейти к следующему независимому этапу.

- [ ] **Этап 2 — роли доступа** — БЛОКИРУЕТ этап 7, делать раньше него
  В коде сейчас нет ничего (`app_roles`/`requireRole` не существуют). В проде две Directus-роли: «Чтец» и «Administrator».
  **Решение Игоря от 2026-07-25:** создавать и править сетлисты может только отдельная роль `musician_editor` — обычный «Чтец» сетлисты только читает. Значит ролевая модель нужна ДО сетлистов.
  Осталось решить (в NEEDS DECISION): как роль попадает в сессию (отдельная Directus-роль ↔ policy, или поле в профиле?), кто её назначает, как гейтить BFF-роуты (`requireRole`), нужны ли ещё роли кроме `musician_editor`.
  **Учти:** права Directus сейчас НЕ являются рабочим контуром контроля доступа — приложение ходит в Directus админ-клиентом через BFF. Реальный гейт обязан быть в BFF-роутах, права Directus — второй рубеж.

- [ ] **Этап 6 — обобщение outbox**
  Сейчас `OutboxRecord` (`src/shared/offline/db.ts`) жёстко под прогресс чтения (`op: 'single'|'batch'`, `dayIds`, `count`). Для этапов 7/9/10 нужны другие типы записей. Требований в спеке нет; форма обобщения — архитектурное решение. Тронет офлайн-контур ⇒ полный production-контур верификации обязателен.

- [ ] **Этап 7 — сетлисты**
  Спека упоминает `setlist_items.key`/`.capo` (§10.1, §10.4) и скоуп настроек (§7), но UI/флоу сетлистов не описан вовсе.
  **Состояние прод-Directus на 2026-07-25 (проверено):** коллекции `setlists`/`setlist_items`/`song_user_state` есть (uuid PK, FK на `songs` = integer, в `song_user_state` поле называется `user_id`, не `user`). Права в policy `custom`:
  - `setlists`, `setlist_items` — `read` всех записей. Записи (create/update/delete) НЕТ ни у кого кроме админа.
  - `song_user_state` — `create` + `read`/`update`/`delete` своих (`user_id = $CURRENT_USER`). Настроено полностью, менять не нужно.
  Поля `setlist_items`: `sort`, `key`, `capo`, `setlist`→`setlists`, `song`→`songs`. Поля `setlists`: `title`, `date`, `created_by`→`directus_users`, `date_created`.
  **Блокер:** запись в сетлисты по решению Игоря доступна только роли `musician_editor`, которой ещё нет ⇒ **этап 7 нельзя начинать раньше этапа 2**. Права на запись выдаёт Игорь вручную после того, как ролевая модель определена.

- [ ] **Этап 9 — редактирование песен**
  Сейчас BFF только GET. Спека editing-флоу не описывает (§0 лишь констатирует, что его нет). Нужно: кто редактирует (связано с этапом 2), что именно редактируется, конфликты. Запись обязана идти через write-ahead outbox, не прямым POST (офлайн-инварианты `CLAUDE.md`) ⇒ зависит от этапа 6.

---

## Progress Log

Формат: `### YYYY-MM-DD HH:MM UTC — <что сделано>` + что дальше / блокеры. Новые записи — сверху.

<!-- Автономный агент дописывает записи ниже -->

### 2026-07-25 10:41 UTC — Коммит D: режим paged (клавиши, педали, тап, панель)

Сделано:
- `nextPageDelta(key)` в `lib/sheets.ts` — чистый резолвер (`→`/`пробел`/`PageDown` вперёд, `←`/`PageUp` назад, плюс legacy `Spacebar`); юнит-тесты на все три группы клавиш, в e2e это не уезжает.
- Новый `hooks/usePagedFlow.ts`: `flow.scrollTo({left: page*pitch})`, клонирования нет. **Источник истины — позиция потока, а не React-стейт.** Первая версия держала страницу в стейте и двигала поток эффектом — при плавном скролле промежуточные `scroll`-события округлялись обратно в старую страницу, стейт откатывался и сам же возвращал поток назад: листалка не работала вовсе (поймано в браузере, юнит-тестами такое не ловится). Теперь стейт только отражает скролл.
- Панель листалки в `SongView`: кнопки 44×44 (§11), индикатор «2 / 3», дизейбл на краях; тап по правой/левой трети потока (средняя треть — ничего), тап игнорируется при активном выделении текста.
- `PAGER_HEIGHT` + измерение `chromeAboveFlow`: высота страницы теперь вычитает и шапку песни, и панель. Без этого в `paged` появлялся вертикальный скролл (страница была ровно на высоту шапки длиннее экрана) — режим «страница = экран» этого не допускает. Заодно первый лист в `sheets` гарантированно влезает в экран целиком.

Проверено в браузере (1024×768, песня id=1): кнопки вперёд/назад, `PageDown`, `ArrowLeft`, тап по правой и левой трети — шаг ровно `pitch` (768 = 736 + 32 gap), индикатор и дизейбл кнопок совпадают со страницей; средняя треть не листает; вертикального скролла в `paged` нет; `sheets` после правки высоты — 2 листа, пустых 0. На 390px `paged` приведён к `scroll`: панели нет, клавиши не перехватываются. `npm run test` 421 зелёный, `tsc --noEmit`, lint baseline 59/30.

**Не проверено вручную:** реальный пробел — харнес браузера шлёт на «space» пустой `key`, а `type ' '` не порождает keydown. Резолвер покрыт юнитом (`' '` и `'Spacebar'` → вперёд), но живую педаль/пробел стоит ткнуть руками.

Дальше: коммит E (e2e-регрессия раскладки) — по правилу верификации №4 требует `npx playwright install --with-deps chromium` и `.env.test` с кредами тест-аккаунта.

### 2026-07-25 10:26 UTC — Оболочка дашборда расширена до 48rem

**Решение Игоря:** расширять весь дашборд, а не только страницу песни.

- `--app-shell-max-w: 48rem` + класс `.app-shell-width` (`globals.css`) — один источник ширины. `DashboardLayout` (`max-w-md` → `.app-shell-width`), `BottomNavBar`, `FloatingChapterNav`, `UpdateToast` переведены на него: все четыре фиксированных/центрованных элемента обязаны совпадать с оболочкой, иначе бар и плавающие стрелки «съезжают» относительно контента (до этого они держали ту же `max-w-md` вручную — четыре копии одного числа).
- 48rem = iPad portrait во всю ширину; на десктопе остаются поля с `md:shadow-xl`. Текст ридера по-прежнему ограничен своим `max-w-xl` — длина строки чтения не изменилась.
- Эффект на песню: лист 736px вместо 416px, 2 колонки читаемой ширины, та же песня укладывается в 2 листа вместо 3.

Проверено в браузере (1024×820): главная, список песен, календарь, настройки, ридер, песня в `sheets`×2 — оболочка и nav 768px, левый край совпадает, горизонтального скролла нет; на 390px всё как раньше (оболочка 390, nav 390). `npm run test` 409 зелёных, `tsc --noEmit`, lint baseline 59/30.

**Заметка про dev-окружение (стоила времени):** Turbopack отдавал закэшированный CSS-чанк — правка `globals.css` не появлялась в выдаче даже после рестарта. Лечится удалением `.next`. И важное: не переименовывать `.next` в `.next-stale-*` внутри репо — Tailwind v4 сканирует такую папку (она не в `.gitignore`), ловит в ней ANSI-мусор и генерирует битые классы (`Parsing CSS source code failed`, 500 на всех страницах). Переносить наружу.

### 2026-07-25 10:20 UTC — Коммит C: единый multicol-поток и раскладка листами

Сделано:
- `SongView` рендерит **один** `.cproColumn` со всеми секциями (`columns: var(--col-count); column-gap: 2rem`). `SongBlock.tsx` удалён, `parseSongBlocks` маппится в `HtmlSection[]` прямо в `SongView`. Зазор между секциями переехал с flex-gap обёртки на `margin-bottom: var(--section-gap)` у `.cproSongSection`.
- `.song-block-content{overflow-x:auto}` и `.song-blocks-layout` удалены — горизонтального скролла в reflow-модели больше нет (§11/§12). `max-width: 420px` теперь только `[data-mode='scroll']`.
- Новый `lib/sheets.ts` — чистые `pitch(clientWidth, columnGap)`, `sheetCount(maxRight, pitch)`, `sheetPageHeight(viewportContentHeight)`; DOM внутри нет, измерения аргументами. Тесты `sheets.test.ts` закрывают подводные камни 1 и 3 (шаг с gap/без, `maxRight` ровно кратен шагу ⇒ хвостового пустого листа нет).
- Новый `hooks/useSheets.ts` — измеряет источник (`maxSectionRight` по секциям, не `scrollWidth`), считает высоту листа из скролл-контейнера (`viewportRef` из `page.tsx`), перестраивает на смену колонок/плотности/режима/«только текст» и на resize/`orientationchange`/шрифт с дебаунсом 120мс. Высота приезжает CSS-переменной, поэтому измерение двухпроходное: сначала применяем `--page-h`, на следующем проходе меряем разбивку.
- Подводный камень 2 (типографика клона) закрыт разметкой: и схлопнутый источник, и листы лежат внутри одного `.cproSongBody`; тест `SongView.test.tsx` это ассертит (`root.contains(sheets) && root.contains(source)`), плюс `aria-hidden` на источнике в `sheets` (иначе скринридер читает песню дважды).
- Гейт режима: `page.tsx` читает `SONG_WIDE_LAYOUT_QUERY` (та же константа, что прячет контролы) и на узком экране приводит `mode` к `scroll`; там же `headerHidden` форсится в `false` вне `scroll` — автоскрытие шапки меняло бы высоту вьюпорта и пересобирало листы на каждый скролл (решение 3).

Проверено: `npm run test` (409 зелёных, было 396), `npx tsc --noEmit`, `npm run lint` — 59 errors / 30 warnings, ровно baseline (первая версия `useSheets` давала +1 `set-state-in-effect`, убран сброс стейта в эффекте). Браузер (dev, 1024×820 и 390×844, песня id=1, 8 секций):
- 2 колонки, шрифт 17 → 3 листа, секции распределены без пересечений и потерь (Вступление+К1+К2 / К3+Модуляция+К4 / К5+Завершение), левый край всех листов совпадает (304px), пустых листов 0, горизонтального скролла нет;
- 1 колонка, шрифт 28 → 8 листов, пустых 0; живая смена шрифта на 14 через слайдер → 3 листа, пустых 0 (перестройка без перезагрузки работает);
- 390px: сохранённый `mode:'sheets'` приведён к `scroll`, `--col-count: 1`, листов нет.
Офлайн/SW/роутинг не тронуты (данные и маршруты те же) — e2e не требовался, он под коммит E.

**Вопрос Игорю (не блокирует C):** `DashboardLayout` ограничивает всю дашборд-оболочку `max-w-md` (448px), поэтому на планшете лист шириной 416px и две колонки получаются узкими — механика верная, но ширины экрана режим не использует. Расширять оболочку (только для песни? для всего дашборда?) — решение по UI, не техническая деталь.

Дальше: коммит D (`paged`: `scrollLeft += pitch`, клавиши/педали, тап по третям, индикатор «2 / 3» в обоих постраничных режимах; CSS для `paged` уже на месте, навигации нет).

### 2026-07-25 09:55 UTC — ChoiceGroup в shared, раскладочные настройки только на планшете

Коммит 593da5b:
- `ChoiceGroup` вынесен в `src/shared/components/ui/ChoiceGroup.tsx` (был двумя почти одинаковыми копиями в `ReadingSettingsForm` и `SongViewSettings`; между фичами импорт запрещён, поэтому только через shared). Объединённый API: `disabled` на всю группу + `disabledOptions` поштучно. Добавлен `aria-pressed` — его не было ни в одной из копий.
- `mode`/`columns` рендерятся только при `>=640px` (решение Игоря: на телефоне они не имеют смысла, скрываем, а не дизейблим). Подсказка «2 колонки доступны на планшете» удалена за ненадобностью.

**Важно для коммита C:** брейкпоинт вынесен в **одну экспортируемую константу** `SONG_WIDE_LAYOUT_QUERY` (`hooks/useSongViewSettings.ts`). Потребитель раскладки в C обязан читать её же и приводить режим к `scroll` на узком экране — иначе сохранённый на планшете `mode: 'sheets'` включится на телефоне, где контрол скрыт и выключить его нечем.

Проверено: 396 тестов, `tsc --noEmit`, lint чисто; визуально в браузере — `ReadingSettingsForm` не изменился (выравнивание/тема рендерятся как раньше), панель песни на 375px без раскладочных контролов, на 768px с ними.

### 2026-07-25 09:45 UTC — Ревью A+B, фикс чистоты стейта (точка остановки перед C)

Сделано:
- Ревью коммитов A+B по коду (не по коммит-сообщениям).
- Коммит (f91c178): `writeSettings` вынесен из updater'а `setSettingsState` в `useEffect` на `[settings]`; удаление legacy-ключа — в mount-эффект; инициализатор `useState` стал чисто читающим. Причина: React вправе повторно вызвать updater/инициализатор (StrictMode, отброшенный concurrent-рендер), а в проекте включён `reactCompiler: true`. Обновлён `e2e/offline/README.md` — экспорт `.env.local` перед `bff:start` больше не нужен.

**Поправка к собственному ревью (важно для доверия к прошлым записям):** в ревью я заявил, что нечистый инициализатор терял миграцию legacy-шрифта в StrictMode. Проверил откатом фикса — **тест проходит и на старом коде**: старая версия удаляла legacy, но сразу писала новый ключ, и второй прогон читал уже его. Воспроизводимого бага в старом коде показать не удалось; реальным остаётся только нарушение контракта чистоты (риск записи незакоммиченного состояния при отброшенном рендере). Тест на StrictMode оставлен, но помечен как smoke — разницы между реализациями он не ловит.

Проверено: `npm run test` (395 зелёных), `npx tsc --noEmit`, `npm run lint` (только preexisting в `useSong.ts`/`useSongs.ts`).

**Решение Игоря:** контролы `mode`/`columns` НЕ прячем, хотя они пока ничего не делают (раскладку включает коммит C). Значит B в одиночку не деплоится — C должен идти следом.

Не закрыто из ревью (осознанно, не блокеры C):
- `server/src/loadEnv.ts` — относительный путь `.env.local`, резолвится от `process.cwd()`; запуск не из корня репо тихо не найдёт файл.
- В плане на коммит A записан `aria-hidden` на аккорде — не сделан; `display:none` и так убирает из a11y-дерева, поправить надо план, а не код.
- `ChoiceGroup` дублируется между `SongViewSettings` и `ReadingSettingsForm` — кандидат в `shared/components/ui` (FSD запрещает импорт между фичами).
- Нет юнит-тестов на `data-density`/`showHeader` в `SongView` и на `useMediaQuery`.
- Предсуществующее: в режиме «только текст» строки из одних аккордов (ВСТУПЛЕНИЕ/ПРОИГРЫШ) остаются пустыми строками с разделителями — по смыслу §3.3 их логичнее скрывать целиком, нужно решение Игоря.

Дальше: коммит C (multicol/sheets) — `lib/sheets.ts` (чистые `pitch`/`sheetCount` + юнит-тесты на оба подводных камня §4), `hooks/useSheets.ts`, перестройка `SongView` на один `.cproColumn`, удаление `SongBlock.tsx`, снятие `overflow-x:auto` с `.song-block-content`.

### 2026-07-25 09:30 UTC — Этап 3, коммиты A+B готовы

Сделано:
- Коммит A (c59c19a): `fontSize`/`hideChords` больше не прокидываются пропами через SongView → SongBlock → ChordProHtmlColumn → LineRenderer → ChordRenderer. Вместо этого: `--lyric-size` CSS-переменная на `.cproSongBody` (потомки уже наследуют через em-каскад — отдельная `--chord-size` не понадобилась), `data-chords="off"` + `[data-chords="off"] .chord{display:none}` для режима «только текст». `break-inside: avoid` добавлен на `.cproSongLine` (раньше был только на секции).
- Коммит B (8a8d135): `useSongViewSettings` — единый JSON-ключ `songs:view-settings` (mode/columns/fontSize/density/showChords/showHeader), одноразовая миграция из `songs:font-size` (с немедленной записью нового ключа — без этого миграция теряла значение при повторном маунте). `SongFontSettings` → `SongViewSettings` (слайдер + 3 сегмента + 2 тумблера). Новый generic `useMediaQuery` (`shared/hooks`) — «2 колонки» дизейблится под 640px. `SongView` применяет `density`/`showHeader`; `mode`/`columns` персистятся, но раскладку ещё не меняют (это коммит C).
- Побочный фикс вне плана: BFF (`server/src/index.ts`) никогда не подхватывал `.env.local` (отдельный tsx-процесс, не Next) → `npm run bff:dev` тихо работал без Directus-конфига, login падал `ECONNREFUSED`. Добавлен `server/src/loadEnv.ts` (dotenv, гейт по `NODE_ENV!==production`). `npm run dev` теперь поднимает Next+BFF вместе через `concurrently` — отдельный `bff:dev` остался для раздельного запуска. **Важно:** первая попытка сделать это была через `npm install`, что зашумило `yarn.lock` (проект на Yarn, не npm) — откачено, переустановлено через `yarn add -D concurrently`. Проверять пакетный менеджер перед `npm install` в этом репо впредь.

Проверено: `npm run test` (394 зелёных), `npx tsc --noEmit`, `npm run lint` (только preexisting warnings в других файлах), ручная проверка в браузере (логин, список песен, открытие песни, все тумблеры/слайдер панели настроек, персист между песнями/перезагрузкой). Offline/SW/роутинг не тронуты — e2e не требовался.

Дальше: коммит C (multicol/sheets — `lib/sheets.ts` + `useSheets` + перестройка `SongView` на один `.cproColumn`, `SongBlock.tsx` удаляется).

### 2026-07-25 08:53 UTC — Этап 1 завершён (вручную, режим переведён на ручной)

Сделано:
- Коммит A (7b66d79): удалён мёртвый табличный рендер-путь — `TableLineRenderer.tsx`, `shouldUseTableMode`, `renderMode`/`showDebugHeights` в `ChordProHtmlColumn`, `offsetHeight`-измерение, `structurizeSong`+интерфейсы, осиротевшие `decomposeLine`/`DecomposedLine`/`escapeHtml`/`isChordSymbol` в `chordProUtils`, dead CSS `.cproTableLine`/`.cproSpecialSymbol`, мёртвые пропы `isActive`/`onClick`/`isContinuation` в `SongBlock`. −642 строки.
- Коммит B (0f21ce9): `getSongsList` теперь запрашивает `tempo`/`time` (были всегда undefined в списке) + юнит-тест `songsServer.test.ts`.
- Влит `main` в `chordpro-viewer` (merge eebbb8f) — подтянут security-фикс прокси (89088c4), которого на этой ветке не было.

Проверка: `npm run test` → 388 зелёных. Lint по тронутым файлам — чисто, кроме pre-existing `@ts-nocheck` в `songsServer.ts` (baseline, не новый). Offline/SW/роутинг не тронуты — e2e не требовался.

**Режим работы:** переведён на РУЧНОЙ. Автономная routine (trig_01VaTmwsNGcbMbKVmgRmya8s) ВЫКЛЮЧЕНА (первый прогон 04:23 UTC не дал ни коммита, ни записи в лог). Дальше этапы делает Игорь в сессиях с Claude, не автономно.

Дальше: этап 3 (раскладка/колонки/настройки §4+§5+§7+§3.3+§11) — объёмный, начинать со свежего контекста. Заблокированные этапы 2/6/7/9 — требований в спеке нет, нужны решения Игоря (роли, editing-флоу). Этап 7 (сетлисты) зависит от этапа 2 (роль musician_editor).
