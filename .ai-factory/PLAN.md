# План: настройки чтения на странице «Настройки» и общий компонент

Branch: main  
Created: 2026-04-23

## Settings

- Testing: no (в репозитории нет `*.test.ts(x)`; при появлении тестового каркаса — добавить снимок для формы)
- Logging: verbose
- Docs: no

## Roadmap Linkage

Файл `.ai-factory/ROADMAP.md` отсутствует — привязка к вехам не выполнялась.

## Контекст и уточнение

- **Цель:** вынести UI настроек чтения из монолита `ReadingSettings.tsx` в переиспользуемый компонент; на `src/app/dashboard/settings/page.tsx` добавить раздел «Настройки чтения» (по желанию — свёрнутый, например `<details>` / раскрывающийся блок в стиле приложения).
- **Профиль:** отдельного `src/app/dashboard/profile/page.tsx` в проекте нет. Если имелась в виду «страница пользователя», фактическая точка входа — уже существующая `settings/page.tsx`; отдельный компонент формы позволит позже вставить тот же блок на страницу профиля без дублирования разметки.
- **Чтение:** `ReadingView` сегодня открывает `ReadingSettings` как `BottomSheet`. После рефакторинга логично оставить шестерёнку и нижний лист, но **наполнение** — общая форма, чтобы не дублировать поля.

## Tasks

### Phase 1: Общий компонент формы

- [x] **Task 1: Вынести разметку полей в `ReadingSettingsForm`**

  **Доставка:** новый клиентский компонент (например `src/features/reading/components/ReadingSettingsForm.tsx`) с пропсами `settings: ReadingSettings`, `onSettingsChange: (s: ReadingSettings) => void`, опционально `className` / `disabled` для полей при сохранении.

  **Поведение:** та же логика, что сейчас внутри `BottomSheet` в `ReadingSettings.tsx` (слайдеры, кнопки выравнивания/темы, селекты переводов, тумблер стихов, нормализация значений перевода через `getSelfHostedTranslationOptions`).

  **Файлы:** создать `ReadingSettingsForm.tsx`; править `ReadingSettings.tsx` (импорт формы, обёртка `BottomSheet` + заголовок).

  **LOGGING REQUIREMENTS:** без `console` в презентационном слое; при необходимости отладки на этапе разработки — временный `DEBUG` только в местах вызова API (см. Task 3), формат `[ReadingSettingsForm]`, убрать перед merge или оставить за флагом `process.env.NODE_ENV === 'development'`.

### Phase 2: Страница настроек дашборда

- [x] **Task 2: Секция «Настройки чтения» на `settings/page.tsx`**

  **Доставка:** на `src/app/dashboard/settings/page.tsx` подключить `useReadingSettings`, показать скелетон/«Загрузка…» пока `isLoading`, при ошибках загрузки — краткое сообщение пользователю (и `console.error` / `WARN` с контекстом).

  **UI:** подсекция с заголовком «Настройки чтения»; «спрятать» — реализовать через `<details open>` по умолчанию **или** свёрнутый по умолчанию `<details>` — выбрать то, что лучше согласуется с UX (рекомендация: по умолчанию **свёрнуто**, чтобы не перегружать экран темой приложения).

  **Сохранение:** при каждом изменении вызывать существующий `updateSettings` (как в `ReadingView.handleSettingsChange`), с дебаунсом **опционально** — только если заметны лишние запросы при движении слайдера; иначе оставить текущую семантику «сохранить сразу».

  **Файлы:** `src/app/dashboard/settings/page.tsx`; при росте разметки — вынести обёртку секции в `src/features/reading/components/DashboardReadingSettingsSection.tsx` или `src/features/settings/...` по принятой в проекте структуре.

  **LOGGING REQUIREMENTS:**
  - `INFO [settings/reading]` при успешной загрузке настроек (без PII, только факт и длительность по желанию).
  - `WARN [settings/reading]` при падении `getSettings` / невалидных значениях с сервера.
  - `DEBUG [settings/reading]` перед/после `updateSettings` с кратким перечнем изменённых полей (без полного текста Библии).

### Phase 3: Проверка и согласованность

- [x] **Task 3: Регрессия сценариев**

  **Проверить вручную:**
  1. `/dashboard/settings` — секция чтения, сохранение, перезагрузка страницы — значения те же.
  2. Экран чтения — шестерёнка, `BottomSheet`, изменения сохраняются и отражаются в тексте/теме.
  3. Нет двойной регистрации хуков: на странице настроек один экземпляр `useReadingSettings`; в `ReadingView` — как сейчас (отдельный экземпляр допустим: данные подтягиваются с API при входе).

  **LOGGING REQUIREMENTS:** при обнаружении рассинхрона кэша — залогировать `WARN` с указанием экрана (settings vs reading).

## Следующие шаги

План в режиме **fast** (`paths.plan`).

Для реализации:

```text
/aif-implement
```

Контекст для имплементации:

- Plan file: `.ai-factory/PLAN.md`
- Testing: no
- Logging: verbose
- Docs: no

Освободить контекст при необходимости: `/clear` или `/compact`.
